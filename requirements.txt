Django==4.2.30
mysqlclient==2.2.8
python-decouple==3.8
python-dateutil==2.9.0.post0
reportlab==5.0.0

# Transitive dependencies (installed automatically by the packages above,
# pinned here too so `pip install -r requirements.txt` reproduces the
# exact same environment on any machine)
asgiref==3.11.1
charset-normalizer==3.4.9
pillow==12.3.0
six==1.17.0
sqlparse==0.5.5
tzdata==2026.3
